package com.example.elaborato_mobile.ui.theme

import androidx.compose.ui.graphics.Color


val GreenPrimaryDark = Color(0xFF4CAF50) // Medium Green
val GreenSecondaryDark = Color(0xFF8BC34A) // Lime Green
val GreenBackgroundDark = Color(0xFF121212) // Dark Background
val GreenCardBackgroundDark = Color(0xFF1A1A1A) // Darker Card Background
val GreenTextPrimaryDark = Color(0xFFD0E8A5) // Light Green for Primary Text
val GreenTextSecondaryDark = Color(0xFFA4B65C) // Olive Green (lighter for dark theme)
val GreenDividerDark = Color(0xFF2C2C2C) // Darker Mint Green for dividers
val RedErrorDark = Color(0xFFFF7043) // Soft Coral
val GreenDisabledDark = Color(0xFF5D6F59) // Muted Green for Disabled
val GreenIconDark = Color(0xFF81C784) // Light Forest Green for icons


val BluePrimary = Color(0xFF3F51B5) // Deep blue for primary elements (classic and professional)
val BlueSecondary = Color(0xFF536DFE) // Lighter blue for secondary elements (still in the blue family)
val AccentOrange = Color(0xFFFFA000) // Warm orange accent (not overpowering but vibrant)
val AccentGreen = Color(0xFF4CAF50) // Vibrant green for success messages or buttons

// Backgrounds and Surfaces
val BackgroundLight = Color(0xFFF5F5F5) // Very light grey background (clean and neutral)
val SurfaceBackground = Color(0xFFFFFFFF) // White surface for cards, buttons, etc.

// Text Colors
val TextPrimary = Color(0xFF212121) // Dark grey for primary text (great contrast)
val TextSecondary = Color(0xFF757575) // Medium grey for secondary text (less intense)
val TextOnLightBackground = Color(0xFFFFFFFF) // White text on dark elements (for high contrast)

// Error and Disabled Colors
val ErrorRed = Color(0xFFD32F2F) // Standard error red (bold and clear)
val DisabledGrey = Color(0xFFBDBDBD) // Muted grey for disabled states (visually distinct but not too jarring)

// Icons
val IconPrimary = Color(0xFF3F51B5) // Primary icons use the primary blue color for consistency
val IconAccent = Color(0xFFFFA000) // Accent icons use orange for some fun visual pop